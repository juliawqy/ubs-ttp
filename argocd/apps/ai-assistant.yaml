apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ubs-ttp-ai-assistant
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_ORG/ubs-ttp.git
    targetRevision: HEAD
    path: k8s/overlays/staging/ai-assistant
  destination:
    server: https://kubernetes.default.svc
    namespace: ubs-ttp-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
