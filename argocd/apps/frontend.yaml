apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: aequus-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_ORG/aequus.git
    targetRevision: HEAD
    path: k8s/overlays/staging/frontend
  destination:
    server: https://kubernetes.default.svc
    namespace: aequus-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
