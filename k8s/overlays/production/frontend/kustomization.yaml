apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ubs-ttp-production
resources:
  - ../../../base/frontend
images:
  - name: PLACEHOLDER
    newName: ${AWS_ACCOUNT_ID}.dkr.ecr.ap-southeast-1.amazonaws.com/ubs-ttp-frontend
    newTag: latest  # replaced by deploy-prod pipeline
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
    target:
      kind: Deployment
      name: frontend
