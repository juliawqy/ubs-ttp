apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ubs-ttp-staging
resources:
  - ../../../base/training
images:
  - name: PLACEHOLDER
    newName: ${AWS_ACCOUNT_ID}.dkr.ecr.ap-southeast-1.amazonaws.com/ubs-ttp-training
    newTag: latest  # replaced by CI pipeline
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
    target:
      kind: Deployment
      name: training
